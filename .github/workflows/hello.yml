name: Sample Flow

on:
  push:
    branches:
      - 'master'

jobs:
  checkoutEcho:
    name: Test expected failure
    runs-on: ubuntu-latest
    steps:
    - name: Fake command 
      run: lol idk
      continue-on-error: true
    - name: Test response
      if: failure()
      run: exit 1

