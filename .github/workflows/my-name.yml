name: Hello
  
on:
  pull_request:
    branches:
      - 'master'

jobs:
  hello:
    name: Run hello
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Hello
      uses: ./.github/actions/hello
      env:
        HELLO: ${{ secrets.MY_NAME }}
